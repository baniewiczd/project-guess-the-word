const words = ["Regenschirm", "Sonnenbrille", "Tierheim"];
    
console.log(words);

class GuessTheWordGame {
    constructor(words){
        this.words = words;
        this.pickedWord = "";
    }
}

//var index = Math.floor(Math.random()*this.words.length);
//const wordToGuess = this.words[index];
//const wordLength = wordToGuess.length;
